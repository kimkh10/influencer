새 제품의 매핑을 index.html에 추가합니다.

사용자에게 다음 정보를 질문하세요:
1. 스프레드시트에 있는 타이틀 키워드 (예: "프로바이오틱스")
2. 캘린더에 표시할 짧은 이름 (예: "분홍이")
3. 제품 설명 문구 (예: "인기템 깜짝공구\n단 3일!") — 줄바꿈은 \n으로
4. 배지 텍스트 (예: "HOT", "품절대란템") — 없으면 빈 값

모든 정보를 받은 후 index.html에서 다음 함수들에 매핑을 추가하세요:
- `getShortName()` 의 map 배열에 [키워드, 표시이름] 추가
- `getProductDesc()` 의 map 객체에 키워드: 설명 추가
- `getBadge()` 에 배지가 있으면 if문 추가

수정 완료 후 `vercel --yes --prod`로 배포까지 실행하세요.
